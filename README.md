# py_geo_fd

## Install

```
pip install -e .
```

## Run

Once properly installed as described above, you can import the functionalities into your python scripts by:

```
from py_geo_fd import Stent, load_config
```
